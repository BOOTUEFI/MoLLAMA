CONTINUE BUT TAKE NOTES...
i cant save any of the agents i create or the defualt ones i modify, same goes for skills and routines.
Skills are like prompt commands that i can call through the main chat like /frontend-design or /backend-design or etc
Still cant choose the agents i want to use in the warroom
And the projects section is more tailored towards creating nextjs, python aka coding projects so i need to be able to create, modify, delete files and folders. and upload them. (so same as the tool editor)

also create a main.py in the mollama root where i can run the frontend and backend in one go without having to use docker.
and also id like to be able to choose in the main chat and projects a existing folder where the agents will work.

And the context bar is still having problems where it shows 8k context for some reason.

And the maintenance console doesnt work very well, can we just make it snap to predefined points like topleft topright topcenter etc etc and when it opens/closes it knows how to do the open and close cause if put it right now at the bottom right the minimized state is way above the bottom right corner but when i open it its perfect...
Overall its a pretty buggy experience with the maintenance console.

tools editor "chat" should be removed, we only need the agent.

also i dont understand why we got "orchestrator" when the mollama smart model IS SUPPOSED TO BE THE ORCHESTRATOR.
Also need to add think tags support.
tool editor we can create folders and create files inside folders for some reason.

navbar should be rounded on all corner, not only on the left ones and should be on the right side of the screen, aka after the right panel

And live feed aint working anymore and same goes for the "working" part of the nodes, its probably cause the admin urls dont update those parts.